function okClicked() {
  // Recarregar a página
  location.reload();
}

function cancelClicked() {
  // Fechar o pop-up
  window.close();
  // document.getElementById('popup').style.display = 'none';    //LIMPA O POP UP, Ñ FUNCINANDO
}